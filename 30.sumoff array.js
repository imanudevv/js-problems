//find sum of array elements
let numbers = [1, 2, 3, 4, 5];

//initialize a varaible to store the sum
let sum = 0;

//loop through the array using forEach
numbers.forEach((Number) => {
    sum += number;
});

//print the result
console.log(sum);